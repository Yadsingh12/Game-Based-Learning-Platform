[build]
  base    = "game-platform-next"
  command = "prisma generate && next build --webpack"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"